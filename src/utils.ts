import { ViewName } from "./types";

// Definición de los botones de navegación
export const navigationItems: { view: ViewName; label: string }[] = [
  { view: 'servicios', label: 'Servicios' },
  { view: 'proyectos', label: 'Proyectos' },
  { view: 'contacto', label: 'Contacto' }
];